{
  "dependencies": {
    "@nestjs/mapped-types": "^2.0.2"
  },
  "name": "next-nest",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "pnpm run --parallel dev",
    "lint": "pnpm run --parallel lint",
    "start": "pnpm run --parallel start",
    "format:server": "pnpm run --filter=server format",
    "start:server:prod": "pnpm run --filter=server start:prod",
    "doc:server": "pnpm run --filter=server doc",
    "dev:server": "pnpm run --filter=server dev",
    "dev:client": "pnpm run --filter=client dev",
    "build": "pnpm run --parallel build",
    "build:server": "pnpm run --filter=server build",
    "build:client": "pnpm run --filter=client build",
    "lint:server": "pnpm run --filter=server lint",
    "lint:client": "pnpm run --filter=client lint"
  },
  "keywords": [],
  "author": {
    "name": "Sharieaz Kavier",
    "email": "shahriazkobir@gmail.com"
  },
  "license": "ISC",
  "engines": {
    "node": ">=18.x.x",
    "npm": ">=8.x.x"
  },
  "devDependencies": {}
}
